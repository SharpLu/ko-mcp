{
  "name": "ko-mcp-server",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "registry:refresh-pin": "node scripts/refresh-upstream-pin.mjs",
    "registry:check-upstream": "node scripts/check-upstream-pin.mjs",
    "golden:gate": "node scripts/golden-gate.mjs",
    "golden:capture": "node scripts/golden-capture.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250306.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.9",
    "wrangler": "^4.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SharpLu/ko-mcp",
    "directory": "server"
  }
}
